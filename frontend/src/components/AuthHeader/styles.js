import { StyleSheet } from "react-native";

export default StyleSheet.create({
  headerContainer: {
    flexDirection: "row",
    alignItems: "center",
    marginBottom: 70,
  },
  backButton: {
    width: 26,
  },
  backButtonText: {
    fontSize: 40,

    color: "black",
  },
  headerTitle: {
    fontSize: 26,

    fontWeight: "500",
    paddingHorizontal: 80,
  },
});
