import { StyleSheet } from "react-native";

export default StyleSheet.create({
  headerContainer: {
    flexDirection: "row",
    alignItems: "center",
    marginBottom: 70,
  },
  backButton: {
    width: 26,
  },
  backButtonText: {
    fontSize: 40,

    color: "black",
  },
  headerTitle: {
    fontSize: 20,

    fontWeight: "bold",
    paddingHorizontal: 80,
  },
});
